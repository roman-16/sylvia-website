{
  "name": "sylvia-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tailwindcss --input src/tailwind.css --output public/assets/css/site.css --minify",
    "dev": "trap 'kill 0' EXIT; tailwindcss --input src/tailwind.css --output public/assets/css/site.css --watch & PORT=3000 serve public --no-clipboard --no-port-switching"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "serve": "^14.2.6",
    "tailwindcss": "^4.3.3"
  }
}
